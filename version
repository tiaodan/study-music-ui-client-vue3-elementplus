############# v0.1
-- 跑通一个产品(本地) v0.1 -> 前后端能通就行，能放歌就行，不管自己写的还是别人的 √
1. 服务器安装 tailscale，并登录 √
2. 手动下载一个歌手音乐 (mp3+歌词(歌词可以之后下)) - 2首就行
4. 找一个第三方项目，前后端都有 ：https://github.com/Yin-Hongwei/music-website
	- 下载到本地能播放
	

############# v0.2
-- 跑通一个产品(本地) v0.2 ->  该别人项目，实现自己需求(仅 ui-client)
4. 做一个简单网站，实现播放音乐+歌词 (可从github找) (或者试验下clode code) 
	- 参考现有项目，只实现我需要的，别的代码，可以留着，可以删了，建议先留着，没准以后用呢
	- 前端-muisic，只要播放，歌词、搜索(歌手/歌曲)、歌手列表。歌曲展示-随机、去除异常后的大弹窗、去除报错
		-- 有个问题，请求都是 206， 需要再点一次才播放
		-- 插件版本用最新版
		-- 页面
			-- 登录 不要 x 
			-- 注册 不要 x 
		-- 步骤 
			-- 1. 依次把插件版本装成最新版，能运行。并且 npm audit 没有警告 high的。npm run serve能运行 ？？
					-- 这个太难了，什么项目都有漏洞，不好实现 -》 先不管，等熟悉了项目，再单独拿出来改
			-- 2. 从main.js开始，只保留自己需要的，不用的改成vue.bak
				从大到小
				-- 剔除不要的大界面: （不要删js 里方法，或者变量，以免影响其它页面。我就遇到了）
					-- 登录、注册、个人界面，包括主界面功能按钮，都删了 √
							-- view/YinHeader - 按钮                  √
							-- router/index.ts 删除  登录、注册、个人 √
					-- 主界面-系统名字 enmus/music-name.ts        √
					-- 报错界面，影响操作，隐藏。打印错误到控制台 √  vue.confjg.js -> client-overlay:false
					-- 加一个单曲循环功能                         √ 
					-- 按钮为播放时，切歌，添加歌曲后播放，单曲循环播放完后，容易失灵，需要再点击一下才行。
								-》还是不行。先不管了
					-- 首页、以前的内容，不显示，这个界面还保留着 √ 删除Home.vue template里内容
					-- 歌单，整个不显示。表头按钮也不显示         √ 删除 enums/nav.ts // 左侧导航栏-里歌单内容
	- 后端，AI把java 翻译成go后，再根据自己已有项目，做修改 -》没弄
		-- 先单独实现ui-client后，再改后端，控制变量法。防止改错后，自己不会弄了
	- 前端-manage ,还没想好 -》没弄
	  -- 先单独实现ui-client后，再改后端，控制变量法。防止改错后，自己不会弄了